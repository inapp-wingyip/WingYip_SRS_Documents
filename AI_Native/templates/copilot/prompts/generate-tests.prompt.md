# Generate Tests from Spec

Generate test skeletons for all acceptance criteria in #file:SPEC.md.

- Use the appropriate test framework for the stack (xUnit for C#, Vitest for TS).
- Include Arrange/Act/Assert comments for each test.
- Save tests to the standard test directory for the repo.
- Run the tests to confirm they compile (they will fail until implemented).
